import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  mapContainer: {
    height: 'calc(100vh - 100px)',
    minHeight: '600px',
    width: '100%',
    borderRadius: '10px',
    overflow: 'hidden',
  },
}));
